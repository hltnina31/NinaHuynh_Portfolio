import { Box, Container, Typography } from "@mui/material";

export default function Experience() {
  return (
    <Box component="section" id="timeline" sx={{ py: 8 }}>
      <Container maxWidth="lg">
        <Typography component="h2" variant="h2">
          Tidslinje
        </Typography>
      </Container>
    </Box>
  );
}
